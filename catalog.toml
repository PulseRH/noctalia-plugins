# This file is auto-generated from */plugin.toml. Do not edit manually.

[[plugin]]
id = "pulser/active-window-hover"
name = "Active Window (hover expand)"
version = "0.4.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "app-window-filled"
description = "Shows the focused window's title via wlrctl (wlr-foreign-toplevel-management), with the real per-app icon (via its .desktop entry + icon theme, rasterized/cached with ImageMagick) when one can be found. Compact/expand text width and icon size are both configurable separately, animating between them on hover. Scrolls as a marquee if still too long even expanded. Requires the `wlrctl` CLI tool."
min_noctalia = "5.0.0"
tags = ["window"]

[[plugin]]
id = "pulser/clock-hover"
name = "Clock (hover expand)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "clock"
description = "Compact clock that expands to a full weekday/date/time on hover."
min_noctalia = "5.0.0"
tags = ["clock"]

[[plugin]]
id = "pulser/volume-hover"
name = "Volume (scroll slider)"
version = "0.5.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "volume-high"
description = "Volume as a persistent speaker glyph (Tabler icon, struck-through when muted) with a solid rounded slider that reveals to its right on hover, animating open/close with the same eased timing as the other hover widgets. Scroll over the widget to change volume, left-click opens the audio panel (real draggable slider), right-click toggles mute. Speaker and bar are baked together into one image (in the theme colour) so reveal frames are pre-baked and swapped per animation step. Needs ImageMagick; without it, falls back to a plain glyph + percentage."
min_noctalia = "5.0.0"
tags = ["audio"]

[[plugin]]
id = "pulser/sysmon-cpu"
name = "CPU (mini)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "cpu-usage"
description = "Compact gauge bar or plain percentage (toggle below); hover shows percent, clock speed, and package wattage."
min_noctalia = "5.0.0"
tags = ["system"]

[[plugin]]
id = "pulser/sysmon-ram"
name = "RAM (mini)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "memory"
description = "Compact gauge bar or plain percentage (toggle below); hover shows percent and used/total GB (total auto-detected)."
min_noctalia = "5.0.0"
tags = ["system"]

[[plugin]]
id = "pulser/sysmon-temp"
name = "CPU Temp (mini)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "cpu-temperature"
description = "CPU package temperature always shown; hover can also show GPU, hottest NVMe, RAM (DIMM), and hottest CPU core, each toggleable below."
min_noctalia = "5.0.0"
tags = ["system"]

[[plugin]]
id = "pulser/sysmon-net-down"
name = "Download speed (mini)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "download-speed"
description = "Auto-detects the busiest interface. Hides entirely below an idle threshold (KB/s -- 1024 = 1 meg); hovering always shows it. Compact units (K/M) can be toggled off for full KB/s-MB/s."
min_noctalia = "5.0.0"
tags = ["system"]

[[plugin]]
id = "pulser/sysmon-net-up"
name = "Upload speed (mini)"
version = "0.1.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "upload-speed"
description = "Auto-detects the busiest interface. Hides entirely below an idle threshold (KB/s -- 1024 = 1 meg); hovering always shows it. Compact units (K/M) can be toggled off for full KB/s-MB/s."
min_noctalia = "5.0.0"
tags = ["system"]

[[plugin]]
id = "pulser/media-mini"
name = "Media (mini)"
version = "0.9.0"
plugin_api = 5
author = "pulser"
license = "MIT"
icon = "player-play-filled"
description = "Compact now-playing (artist - title) via playerctl/MPRIS, preferring whichever player is actually Playing over a paused/stale one, with rounded cover art (including a YouTube-thumbnail fallback for browser MPRIS, which doesn't expose real art) and an image-rendered playback-progress bar beside it (both toggleable below). Compact/expand text width, cover size, and rounding are all configurable settings. Scrolls as a marquee on hover if still too long even expanded; keeps polling while scrolling so a track change is never missed. Left-click toggles play/pause, right-click opens the media panel, and scrolling over the widget skips tracks (scroll up = previous, scroll down = next)."
min_noctalia = "5.0.0"
tags = ["media"]
